<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.krasus1966.news.user.mapper.FansMapper">

    <!-- 开启二级缓存 -->
<!--    <cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>-->

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="top.krasus1966.news.entity.Fans">
        <result column="id" property="id" />
        <result column="writer_id" property="writerId" />
        <result column="fan_id" property="fanId" />
        <result column="face" property="face" />
        <result column="fan_nickname" property="fanNickname" />
        <result column="sex" property="sex" />
        <result column="province" property="province" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,
        writer_id, fan_id, face, fan_nickname, sex, province
    </sql>

</mapper>
